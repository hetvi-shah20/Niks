<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.niks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Setting" android:parentActivityName=".Navigation"></activity>
        <activity
            android:name=".Navigation"
            android:label="NIKS"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ChangePassword"  android:parentActivityName=".Setting" />
        <activity android:name=".EditProfile"  android:parentActivityName=".Setting"/>
        <activity android:name=".Profile"  android:parentActivityName=".Navigation"/>
        <activity android:name=".ForgotPassword" android:parentActivityName=".Navigation" />
        <activity android:name=".OTPScreen" />
        <activity android:name=".Signup"/>
        <activity android:name=".Login">

        </activity>
        <activity android:name=".SplashScreen" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>